// Root build file. The watch face itself is a resource-only Android module
// (see :watchface) - there is no Java/Kotlin source anywhere in this project.
plugins {
    id("com.android.application") version "8.7.3" apply false
}
