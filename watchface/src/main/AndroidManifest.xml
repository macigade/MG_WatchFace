<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:hasCode="false"
        android:label="@string/app_name">

        <!-- Minimum Watch Face Format version this face needs.
             4 = Wear OS 6 (One UI 8 Watch), which the Galaxy Watch 7 received
             in October 2025. Version 4 is required because the five colour
             palettes drive Font/Fill/Stroke/tintColor through Transform, and
             those attributes only became transformable in 4 - as did
             colorArgb() and extractColorFromColors().

             minSdk deliberately stays at 34: this property is the real gate,
             and raising minSdk to 36 would force the whole toolchain to AGP 9.
             Change both together if you ever want a hard install-time block. -->
        <property
            android:name="com.google.wear.watchface.format.version"
            android:value="4" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <meta-data
            android:name="com.google.android.wearable.watchface.preview"
            android:resource="@drawable/preview" />
    </application>
</manifest>
