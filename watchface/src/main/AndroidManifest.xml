<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.type.watch" />

    <application
        android:hasCode="false"
        android:label="@string/app_name">

        <!-- Minimum Watch Face Format version this face needs.
             2 = Wear OS 5 (API 34), which is what the Galaxy Watch 7 shipped
             with. Raise it only when you start using elements introduced in a
             later format version (3 = Wear OS 5.1, 4 = Wear OS 6). -->
        <property
            android:name="com.google.wear.watchface.format.version"
            android:value="2" />

        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <meta-data
            android:name="com.google.android.wearable.watchface.preview"
            android:resource="@drawable/preview" />
    </application>
</manifest>
